// First-site adapter: the shared shell supplies the command/focus contract.
// A later site may export a different command vocabulary through AppFrame.
export {
  AppCommandMenu as KokoroCommandMenu,
} from "@/components/blocks/app-frame/app-command-menu"
